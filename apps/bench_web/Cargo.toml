[package]
name = "bench-web"
version = "0.1.0"
edition.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
bunny-ui.workspace = true # dom-only: no canvas feature
bunny-ui-web = { path = "../../crates/bunny_ui_web", default-features = false }

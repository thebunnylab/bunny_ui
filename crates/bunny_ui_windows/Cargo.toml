[package]
name = "bunny-ui-windows"
version = "0.1.0"
edition.workspace = true
description = "bunny-ui Windows shell — native window and bitmap blit, dependency-free Win32 FFI"

[dependencies]
bunny-ui.workspace = true

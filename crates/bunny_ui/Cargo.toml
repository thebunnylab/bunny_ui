[package]
name = "bunny-ui"
version = "0.1.0"
edition.workspace = true
description = "Declarative UI in Rust with a SwiftUI spirit — monomorphic typed tree, state with structural identity, incremental reconciliation"

[dependencies]
motor.workspace = true
